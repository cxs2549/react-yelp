import styled from "styled-components"

const StyledSearch = styled.div`
  background-color: lightgreen;
  height: 100vh;
  overflow-y: scroll;
`

const Search = () => {
  return <StyledSearch>yelp!</StyledSearch>
}
export default Search
